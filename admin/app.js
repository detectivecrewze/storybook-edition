(function(){"use strict";const $=(s,n=document)=>n.querySelector(s);const Project=window.StorybookProject;const Themes=window.StorybookThemes;const secretKey="storybook:admin-secret";let secret=sessionStorage.getItem(secretKey)||"";let pendingCreate="";let pendingDelete="";function apiBase(){return (window.STORYBOOK_RUNTIME?.apiBaseUrl||$("meta[name='gift-api-base']")?.content||"").replace(/\/$/,"")}async function request(path,options={}){const response=await fetch(`${apiBase()}${path}`,{...options,headers:{Accept:"application/json","Content-Type":"application/json",Authorization:`Bearer ${secret}`,...(options.headers||{})}});const payload=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(payload.error||`HTTP ${response.status}`),{status:response.status});return payload}function toast(message){const node=$("#toast");node.textContent=message;node.hidden=false;setTimeout(()=>node.hidden=true,2200)}function login(){$("#login-screen").hidden=true;$("#admin-app").hidden=false;load()}function logout(){secret="";sessionStorage.removeItem(secretKey);$("#admin-app").hidden=true;$("#login-screen").hidden=false}function frontend(url){try{const target=new URL(url);if(["localhost","127.0.0.1"].includes(location.hostname)){target.protocol=location.protocol;target.hostname=location.hostname;target.port=location.port}return target.toString()}catch{return url}}function format(value){return value?new Intl.DateTimeFormat("id-ID",{day:"numeric",month:"short",year:"numeric"}).format(new Date(value)):"—"}function renderCard(project){const fragment=$("#project-card-template").content.cloneNode(true);const card=$("article",fragment);$(".status",card).textContent=project.status;$(".status",card).dataset.status=project.status;$(".theme",card).textContent=Themes.getTheme(project.themeId).label;$(".occasion",card).textContent=Project.OCCASION_PRESETS[project.occasionPreset]?.label.id||project.occasionPreset;$(".project-id",card).textContent=project.projectId;$(".recipient",card).textContent=project.recipient||"Belum diisi";$(".sender",card).textContent=project.sender||"Belum diisi";$(".updated",card).textContent=format(project.updatedAt);$(".media-count",card).textContent=project.galleryCount;$(".source",card).textContent=project.source;const studio=frontend(project.studioUrl),gift=frontend(project.giftUrl);$(".open-studio",card).href=studio;$(".open-gift",card).href=gift;$(".open-gift",card).style.pointerEvents=project.status==="published"?"":"none";$(".copy-studio",card).onclick=()=>navigator.clipboard.writeText(studio).then(()=>toast("Studio link disalin."));const archive=$(".archive",card);archive.textContent=project.status==="archived"?"Restore":"Archive";archive.onclick=()=>updateStatus(project.projectId,project.status==="archived"?"restore":"archive");$(".delete",card).onclick=()=>openDelete(project.projectId);$("#project-grid").append(fragment)}async function load(){$("#loading").hidden=false;try{const q=new URLSearchParams({search:$("#project-search").value,status:$("#status-filter").value,limit:"100"});const data=await request(`/api/admin/projects?${q}`);$("#stat-total").textContent=data.stats.total;$("#stat-draft").textContent=data.stats.draft;$("#stat-published").textContent=data.stats.published;$("#stat-archived").textContent=data.stats.archived;$("#project-grid").replaceChildren();data.projects.forEach(renderCard);$("#empty").hidden=Boolean(data.projects.length)}catch(error){if(error.status===403)return logout();toast(error.message)}finally{$("#loading").hidden=true}}async function create(){if(!pendingCreate)pendingCreate=`manual-${crypto.randomUUID?.()||Date.now()}`;try{const result=await request("/api/admin/projects",{method:"POST",body:JSON.stringify({idempotencyKey:pendingCreate})});pendingCreate="";$("#created-studio-url").value=frontend(result.studioUrl);$("#open-created").href=frontend(result.studioUrl);$("#result-dialog").showModal();load()}catch(error){toast(error.message)}}async function updateStatus(id,action){try{await request(`/api/admin/projects/${encodeURIComponent(id)}`,{method:"PATCH",body:JSON.stringify({action})});load()}catch(error){toast(error.message)}}function openDelete(id){pendingDelete=id;$("#delete-project-id").textContent=id;$("#delete-confirmation").value="";$("#confirm-delete").disabled=true;$("#delete-dialog").showModal()}async function remove(){try{await request(`/api/admin/projects/${encodeURIComponent(pendingDelete)}`,{method:"DELETE"});$("#delete-dialog").close();load()}catch(error){toast(error.message)}}$("#login-form").onsubmit=async event=>{event.preventDefault();secret=$("#admin-secret").value.trim();try{await request("/api/admin/projects?limit=1");sessionStorage.setItem(secretKey,secret);login()}catch(error){$("#login-error").textContent=error.status===403?"Admin secret tidak cocok.":error.message}};$("#logout").onclick=logout;$("#refresh-projects").onclick=load;$("#generate-project").onclick=create;$("#project-search").oninput=()=>{clearTimeout(window.__search);window.__search=setTimeout(load,250)};$("#status-filter").onchange=load;$("#copy-created").onclick=()=>navigator.clipboard.writeText($("#created-studio-url").value).then(()=>toast("Link disalin."));document.querySelectorAll("dialog .close").forEach(button=>button.onclick=()=>button.closest("dialog").close());$("#delete-confirmation").oninput=event=>$("#confirm-delete").disabled=event.target.value!==pendingDelete;$("#confirm-delete").onclick=remove;if(secret)login();})();
